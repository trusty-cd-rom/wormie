const host = 'http://wormieapp.herokuapp.com';

const urls = {
	wormholes: `${host}/wormholes`,
	submissions: `${host}/submissions`,
	users: `${host}/users`,
	signup: `${host}/signup`,
	signin: `${host}/signin`,
}

export default urls;